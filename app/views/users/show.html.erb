<p id="notice"><%= notice %></p>

<p>
  <strong><%= @user.username %></strong>
</p>
<p>
  has made <%= @user.ratings.count %> ratings, avarage score <% if @user.ratings.count > 0 %>
      <%= @user.average_rating %>
      <% end %>
</p>

<p>
<!--<ul>
  <% @user.ratings.each do |rating| %>
      <li> <%= rating.to_s %> <%= link_to 'delete', rating, method: :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>
-->
<ul>
  <% @user.ratings.each do |rating| %>
      <li>
        <%= rating %>
        <% if @user == current_user %>
            <%= link_to 'delete', rating, method: :delete, data: { confirm: 'Are you sure?' } %>
        <% end %>
      </li>
  <% end %>
</ul>
</p>
<% if current_user == @user %>
    <%= link_to 'Destroy', user_path(@user), method: :delete, data: { confirm: 'Are you sure?' } %> |
<% end %>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
